apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
  labels:
    app.kubernetes.io/name: argocd-rbac-cm
    app.kubernetes.io/part-of: argocd
data:
  policy.csv: |
    g, devops, role:admin
    g, sysadmin, role:admin
    g, sysadmin@corp.local, role:admin
  policy.default: role:readonly
