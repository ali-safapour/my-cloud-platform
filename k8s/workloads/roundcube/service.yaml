apiVersion: v1
kind: Service
metadata:
  name: roundcube-service
  namespace: mail-system
  labels:
    app.kubernetes.io/name: roundcube
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
  selector:
    app.kubernetes.io/name: roundcube
