apiVersion: v1
kind: Service
metadata:
  name: roundcube-service
  namespace: mail-system
  labels:
    app.kubernetes.io/name: roundcube
spec:
  type: NodePort
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app.kubernetes.io/name: roundcube
