apiVersion: v1
kind: Service
metadata:
  name: openldap-service
  namespace: mail-system
  labels:
    app: openldap
spec:
  type: ClusterIP
  selector:
    app: openldap
  ports:
    - name: ldap
      port: 389
      targetPort: 389
